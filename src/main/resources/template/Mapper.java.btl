package ${package}.modules.${module}.dao;

import AutoBaseMapper;
import ${package}.modules.api.entity.${className}Entity;
import org.apache.ibatis.annotations.Mapper;
import java.util.List;

/**
 * ${comments}
 * @author ${author}<${email}>
 */
@Mapper
public interface ${className}Mapper extends AutoBaseMapper<${className}Entity> {
  List<Long> selectInIds (List<Long> id);
}
